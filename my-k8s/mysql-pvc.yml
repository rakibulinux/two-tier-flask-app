apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  volumeName: mysql-pv
  resources:
    requests:
      storage: 200Mi
  storageClassName: standard
  accessModes:
    - ReadWriteOnce

